# 정기점검 주기 실행 설정.
# cron: "분 시 일 월 요일" (요일 0=일요일 ~ 6=토요일, "*"는 전체 허용, 콤마로 여러 값 지정 가능)
# 예: "0 9 1 * *"  -> 매월 1일 09:00
jobs: []
# 예시:
# jobs:
#   - project_id: lab1_campus
#     cron: "0 9 1 * *"
#     enabled: true
