network:
  mode: "internal"           # internal | vpn (참고용 표시, 로직 분기에는 안 씀)
  pre_flight_check: true
  check_target_node: "Core1"  # ip_allocation.yaml에서 이 노드명의 IP로 대표 점검
  check_port: 22
  check_timeout_sec: 3

ssh:
  connect_timeout_sec: 20
  retry_count: 2
  retry_delay_sec: 5
